"use client";

export default function ArchiveXLogo() {
  return (
    <h1
      style={{
        fontSize: "var(--fs-menu)",
        fontWeight: 800,
        lineHeight: 1,
        letterSpacing: "-0.04em",
        margin: 0,
      }}
    >
      <span>Archive</span>
      <span className="accent-x">X</span>
    </h1>
  );
}